<?xml version="1.0"?>
<!DOCTYPE fontconfig SYSTEM "fonts.dtd">
<fontconfig>

	<!-- Font directory list -->
        <dir>/usr/pkg/share/fonts</dir> 

	<!-- Load per-user customization files stored in modern XDG dir -->
	<include ignore_missing="yes">~/.config/fontconfig/conf.d</include>
	<include ignore_missing="yes">~/.config/fontconfig/fonts.conf</include>

</fontconfig>
